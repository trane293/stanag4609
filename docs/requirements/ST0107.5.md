# MISB ST 0107.5 requirement trace

This trace records the common KLV and Report-on-Change rules used by the
ST 0601 implementation. It accounts for all 20 active requirement identifiers
in the official 21 October 2021 publication. The official SMPTE ST 336:2017
publication is now present locally; its clause-level audit is tracked
separately and is not implied by this MISB-only trace.

| Requirement | Section | Implementation/test | State |
|---|---:|---|---|
| ST 0107.2-01/-02 | 6.1 | big-endian bit and byte order throughout numeric codecs | Verified by official and boundary wire vectors |
| ST 0107.3-03 | 6.2 | MISB Local Set profile of SMPTE ST 336 | Applicable ST 336 subset traced and verified in `SMPTE-ST336-2017.md` |
| ST 0107.3-04 | 6.3 | unknown Local Set items retained/skipped without disrupting known items | Verified |
| ST 0107.3-05 | 6.3.2 | shortest-form BER lengths on encode and strict canonical decode | Verified |
| ST 0107.3-06 | 6.3.3 | exact required numeric value lengths for every fixed-length ST 0601 registry item | Verified registry-wide |
| ST 0107.3-07 | 6.3.3 | maximum value lengths enforced on encode and before decode for every bounded ST 0601 registry item, including all root UTF-8 fields | Verified registry-wide |
| ST 0107.3-08 / ST 0107.5-20 | 6.3.3.1 | big-endian IEEE 754 binary16/32/64 codecs and invalid-width rejection | Verified in ST 1010, ST 1202, and ST 1303 codec tests |
| ST 0107.3-09 | 6.3.3.1 | ST 1201 IMAPA shortest-length selection via `IMAPFieldValue.for_precision`; `ST0601ValidationContext.imap_system_precisions` verifies exact lengths on encode and decode | Verified when producer precision is supplied |
| ST 0107.3-10 | 6.3.3.1 | minimal variable-length unsigned, signed, and timestamp integer encoding; leading extension rejection | Verified for all ST 0601 variable integer fields |
| ST 0107.3-11 | 6.3.3.1 | minimal BER Value encoding | Verified by common canonical BER codec; applicable embedded pack uses are byte-exact tested |
| ST 0107.3-12/-13 | 6.3.3.2 | trimming/control-character validation for every ST 0601 UTF-8 registry item | Verified registry-wide |
| ST 0107.4-14 | 6.3.4 | receiver `expired_tags` after an item exceeds the refresh period | Verified for generic item state |
| ST 0107.4-15 | 6.3.4 | expired values removed from the receiver snapshot | Verified |
| ST 0107.4-16 | 6.3.4 | positive-length ST 0601 Items 1, 2, and 65 | Verified |
| ST 0107.4-17 | 6.3.4 | single-use ZLI immediately removes the last known value | Verified |
| ST 0107.4-18 | 6.3.4 | configurable positive refresh period capped at 30 seconds | Verified |
| ST 0107.4-19 | 6.3.4 | multi-use ST 0601 ZLI rejected by encoder and decoder | Verified |

`ReportOnChangeState` retains multiple instances of one tag as an atomic group.
Identity-aware merging for distributed lists and the specialized lifecycle of
Segment, Amend, and command structures require their own
stateful evaluators and are not claimed here. ST 0601's distributed wavelength
and payload tables are handled separately by `WavelengthTableState` and
`PayloadTableState`; `WeaponsStoresState` handles the distributed Item 140 list
by its four-part physical addresses, and `WaypointListState` handles Item 141
by Waypoint ID while preserving transient reorder conflicts.

`IMAPFieldValue(value, length)` remains available when an application already
owns a validated wire precision. Producers that start with a physical precision
should use `IMAPFieldValue.for_precision(tag, value, precision)` so the library
selects the shortest legal value length using ST 1201 IMAPA rather than relying
on a hand-picked byte count. Relays and receivers that know the originating
system precision can supply the same fact through `ST0601ValidationContext`;
live transports and the verifier select that context per complete packet with
`ST0601ContextProvider`;
the decoder then rejects both undersized values that lose precision and
oversized values that violate shortest-form encoding.
