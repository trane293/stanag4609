# MISB ST 1602.2 requirement trace

Normative source: `ST1602.2.pdf`, dated 2 March 2023. The local publication is
checksum-pinned in `references/standards/manifest.json` and is not distributed.
The machine-readable inventory records the publication's exact ten active
requirement identifiers and empty inactive population. Older `ST 1602` and
`ST 1602.1` prefixes are preserved exactly as printed.

| Requirement or normative behavior | Implementation/test | State |
|---|---|---|
| ST 1602-01; ST 1602-02, ST 0107 and SMPTE Local Set encoding | strict BER/OID Local Set codec, registered 16-byte key, ordered tag-length-value items, and malformed-input tests | Verified for the software-visible wire profile |
| ST 1602-03, mandatory Document Version | required/minimal-uint tests | Verified |
| ST 1602.1-05; ST 1602.1-06; ST 1602.1-07; ST 1602.1-08, mandatory sub-image size and position | missing/domain/signed-integer tests | Verified |
| Optional source-image and AOI geometry | typed variable-width integer round trip | Verified structurally |
| Optional active sub-image geometry | paired dimensions, positive offsets, and rectangle helper tests | Verified structurally |
| Item 17 transparency default and uint8 range | default/all-fields/domain tests | Verified |
| ST 1602-04; ST 1602.1-09, mandatory unique non-zero uint8 Z-Order | required/domain tests plus stateful sibling validation across packets and expiry | Verified, including uniqueness within each composite-image scope |
| ST 1602.1-10, MIIS Core Identifier in every child for multi-sensor imagery | stateful Item 99 child detection across the stream, direct typed Item 94 check, and FMV verifier test | Verified for multi-sensor composites represented by Segment Local Sets |
| Item 1 conditional precision timestamp | uint64 UTC round trip plus `MetadataTreeSnapshot.effective_composite_timestamp` explicit/parent/nested-parent tests | Verified for wire form and receiver-visible inheritance; whether an omitted value truly equals the source-image time is producer-owned |
| ST 0601 Item 99 bridge | nested decode/encode and hierarchy-policy verifier tests | Verified structurally and statefully |
| ST 1607 segmented multi-source composition (§8) | recursive Segment state groups Item 99 children by their immediate parent | Verified for composite Z-order and child-MIIS policies; rendering and producer image truth remain external |

All ten numbered requirements are software-verifiable and have executable
evidence. Source/composite geometry truth, timestamp equality when Item 1 is
omitted, and the visual correctness of the compositing result remain
producer-owned facts.
